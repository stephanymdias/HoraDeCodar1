function executarExercicio4() {
    alert("Área do retângulo: ")

    let altura = parseInt(prompt("Informe a altura: "))
    let base = parseInt(prompt("Informe a base: "))
    soma = altura + base

    alert("O valor da área é: " +soma)
}